# Dependencies
node_modules/
# Lockfiles ARE committed (see backend/ and frontend/) so `npm install`
# resolves the same versions in Docker as on a dev machine. Not ignoring
# yarn.lock/pnpm-lock.yaml either — neither package manager is in use, but
# if that ever changes the resulting lockfile should be committed too.

# Environment
.env
.env.local
.env.*.local

# Build outputs
dist/
build/
.next/
out/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*